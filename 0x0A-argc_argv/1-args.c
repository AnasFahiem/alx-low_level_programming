#include <stdio.h>

/**
 * main - prints its name, followed by a new line
 * @argc: n args
 * @argv: arr args
 * Return: 0
 */

int main(int argc)
{
	if (argc > 0)
		printf("%d\n", argc);
	return (0);
}
